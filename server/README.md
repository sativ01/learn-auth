# Learning OAuth. Server side

Using socket.io for communication with client and Passport library to communicate with OAuth servers

## Available Scripts

In the project directory, you can run:

### `yarn start`

Runs the app in the development mode.<br>
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

